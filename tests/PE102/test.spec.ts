import 'mocha'
import { expect } from 'chai'

import { Client } from '../../src/PE102/Client.js'
import { Server } from '../../src/PE102/Server.js'

/*
let server = new Server()
let client = new Client()

describe('Client class tests', () => {
  it('Client should have a Net Socket', () => {
    expect(client.client).to.be.a('net.Socket')
  })
})

describe('Server class tests', () => {
  it('Server should have a Net Server', () => {
    expect(server.server).to.be.a('net.Server')
  })
})*/
